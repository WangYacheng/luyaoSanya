<Page x:Class="TradeSystem.App.Views.Pages.Test1Page"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:TradeSystem.App.Views.Pages"
      mc:Ignorable="d" Background="#F3F3F3"
      d:DesignHeight="450" d:DesignWidth="800"
      Title="WPF 单元测试演示界面">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <!-- 上部区域：固定高度 -->
            <RowDefinition Height="Auto"/>
            <!-- 下部区域：自动填满剩余空间 -->
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 上部按钮区域 -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <Button Content="开始测试"
                    Command="{Binding ViewModel.StartTestCommand}"
                    Width="120" Height="40" Margin="0,0,10,0"
                    Background="#007ACC" Foreground="White" BorderThickness="0">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="4"/>
                    </Style>
                </Button.Resources>
            </Button>

            <Button Content="清除日志"
                    Command="{Binding ViewModel.ClearLogCommand}"
                    Width="120" Height="40" Margin="0,0,10,0"
                    Background="#6A6A6B" Foreground="White" BorderThickness="0">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="4"/>
                    </Style>
                </Button.Resources>
            </Button>

            <Button Content="导出报告"
                    Command="{Binding ViewModel.ExportReportCommand}"
                    Width="120" Height="40"
                    Background="#28A745" Foreground="White" BorderThickness="0">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="4"/>
                    </Style>
                </Button.Resources>
            </Button>
        </StackPanel>

        <!-- 下部输出区域 -->
        <GroupBox Grid.Row="1" Header="输出信息" FontSize="14" FontWeight="SemiBold">
            <TextBox Text="{Binding ViewModel.LogOutput, UpdateSourceTrigger=PropertyChanged}"
                     IsReadOnly="True"
                     AcceptsReturn="True"
                     VerticalScrollBarVisibility="Auto"
                     Background="White"
                     FontFamily="Consolas"
                     Padding="5"
                     BorderThickness="1"
                     BorderBrush="#DDDDDD"/>
        </GroupBox>
    </Grid>
</Page>
